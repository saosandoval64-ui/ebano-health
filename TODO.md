["Fix error message \"Tú tienes acceso a todo el proyecto","on login**\n   - Need to understand what triggers this message\n   - The login page already has role-based buttons (patient, doctor, admin)\n   - Maybe the error comes from the server action when invalid credentials\n\n2. **[ ] Remove admin access from main login page**\n   - Admin login should only be in footer with separate login page\n   - Update `/login/page.tsx` to only show patient/doctor roles\n   - Create separate admin login access\n\n3. **[ ] Fix scrollbar issue (\"three lines","on scroll)**\n   - The `scrollbar-none` class is used but maybe showing artifacts\n   - Need to check pages with overflow-y-auto\n\n4. **[ ] Each login should have separate access**\n   - Already has `?role=patient` and `?role=doctor` params\n   - Need to ensure admin has its own separate access\n   - Landing page buttons should direct to appropriate login\n\n5. **[ ] Registration: Allow choosing patient/doctor and save separately**\n   - Current `/register/page.tsx` sends to `/register/patient` or `/register/doctor`\n   - These are separate pages which is good\n   - Need to verify they save correctly without mixing data\n   - The `registerUser` action always sets role: \"PATIENT\"\n   - The `registerDoctor` action sets role: \"DOCTOR","Pages to Update:\n- `/app/login/page.tsx` - Remove admin from role selector, handle URL params\n- `/app/page.tsx` - Update landing page to have separate login links\n- `/app/register/page.tsx` - Verify role selection works\n- `/app/register/patient/page.tsx` - Verify patient registration\n- `/app/register/doctor/page.tsx` - Verify doctor registration\n- `/app/actions/auth.ts` - Verify register functions save correctly\n- `/middleware.ts` - Ensure admin routes work properly\n- `/app/globals.css` - Fix scrollbar artifacts\n\n## New Pages to Create:\n- `/app/login/admin/page.tsx` - Separate admin login page"]